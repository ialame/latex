\begin{lstlisting}

# Theta initiale
theta0 = 100
U = theta0 * np.ones(N)
u = np.insert(U, 0, theta0)
u = np.insert(u, N + 1, theta0)
plt.plot(x, u)
for k in range(1, M):
    U = np.linalg.solve((np.eye(N) + dt / (h ** 2) * A), U)
    u = np.insert(U, 0, 0)
    u = np.insert(u, N + 1, 0)
    plt.plot(x, u)

plt.show()
  \end{lstlisting}

